# Technical implementation plans

Complex fixes and multi-file features require a written plan here before application code changes. Copy `IMPLEMENTATION-PLAN-TEMPLATE.md`, name it `implementation--<scope>.md`, complete it, and obtain explicit user approval.

Plans describe intended work; task status continues to live in `docs/dev/fixes/`.
